# Python 3.13.5 was used in the development environment.
#
# CUDA build used locally:
#   torch==2.7.1+cu128
#   torchvision==0.22.1+cu128
# If a CPU-only environment is needed, replace the two lines below with
# the CPU versions from https://pytorch.org/get-started/locally/.
--extra-index-url https://download.pytorch.org/whl/cu128

torch==2.7.1+cu128
torchvision==0.22.1+cu128
numpy==2.1.3
matplotlib==3.10.0
scipy==1.15.3
scikit-image==0.25.0
opencv-python==4.12.0.88
Pillow==11.1.0
tqdm==4.67.1
